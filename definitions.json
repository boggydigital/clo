{
  "version": 1,
  "flags": [
    {
      "token": "help",
      "abbr": "h",
      "hint": "show command line help",
      "desc": "show command line help, including examples where available"
    },
    {
      "token": "verbose",
      "abbr": "v",
      "hint": "show more info when available",
      "desc": "show additional information, like examples where available"
    }
  ],
  "commands": [
    {
      "token": "verify",
      "abbr": "vrf",
      "hint": "verify provided file",
      "desc": "verify that provided file meets expected constraints",
      "arguments": ["path"]
    },
    {
      "token": "embed",
      "abbr": "em",
      "hint": "generate code equal to the definition",
      "desc": "generate code equal to the definition, that can be embedded into the project",
      "arguments": ["path"]
    },
    {
      "token": "boilerplate",
      "abbr": "bp",
      "hint": "generate common code to consume commands",
      "desc": "generate common code to consume commands and provide entry points to handle arguments"
    }
  ],
  "arguments": [
    {
      "token": "path",
      "abbr": "p",
      "hint": "path to the file",
      "env": true,
      "default": true,
      "multiple": false,
      "required": true
    }]
}