{
  "version": 1,
  "env-prefix": "CLV",
  "hint": "declarative CLI parameters",
  "desc": "declarative CLI commands, arguments and flags that can be verified to avoid common errors",
  "flags": [
    {
      "token": "help",
      "abbr": "h",
      "hint": "show command line help",
      "desc": "show command line help, including examples where available"
    },
    {
      "token": "verbose",
      "abbr": "v",
      "hint": "show more info when available",
      "desc": "show additional information, like examples where available"
    }
  ],
  "commands": [
    {
      "token": "verify",
      "abbr": "vf",
      "hint": "verify provided file for common errors",
      "desc": "verify that provided file meets expected constraints and avoids common errors",
      "arguments": ["path"],
      "examples": [
        {
          "arguments": ["path"],
          "values": ["./clove.json"],
          "desc": "verify ./clove.json file for common errors and display a result"
        }
      ]
    }
  ],
  "arguments": [
    {
      "token": "path",
      "abbr": "p",
      "hint": "path to the file",
      "env": true,
      "default": true,
      "multiple": false,
      "required": true
    }]
}